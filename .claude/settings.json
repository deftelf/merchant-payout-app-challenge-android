{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // \"\"'); echo \"$file\" | grep -qE '(build\\.gradle\\.kts|settings\\.gradle\\.kts|libs\\.versions\\.toml)$' || exit 0; cd /home/carl/AndroidStudioProjects/merchant-payout-app-challenge-android && output=$(./gradlew help --quiet 2>&1); status=$?; echo \"$output\"; [ $status -eq 0 ] && exit 0 || exit 2",
            "asyncRewake": true,
            "timeout": 120,
            "statusMessage": "Syncing Gradle..."
          }
        ]
      }
    ]
  }
}
